// Single source of truth for the two panel labels — used as both the
// title and (on the other panel) the "already picked here" exclude label,
// so a rename can't drift out of sync between call sites.
export const CHARACTER_1_LABEL = 'Character #1';
export const CHARACTER_2_LABEL = 'Character #2';
